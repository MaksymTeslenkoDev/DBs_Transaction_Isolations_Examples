SET SESSION TRANSACTION ISOLATION LEVEL SERIALIZABLE;

SET autocommit = 0;

USE store;

START TRANSACTION;

SELECT amount FROM phones WHERE id=1;

DO SLEEP(5);

SELECT amount FROM phones WHERE id=1;

COMMIT;